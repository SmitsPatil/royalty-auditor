# Backend Project
This project holds the FastAPI backend code.

## Structure
- `app/main.py`: Entry point for the FastAPI application.
- `app/db.py`: Database connection (Firebase).
... (other modules in `app/`)
